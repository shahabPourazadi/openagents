# Fresh git history for the public repo

This tree was forked from a private product copy that contained internal URLs, infra identifiers, and commit context. Publishing that history would leak operational detail. The public repo starts with a clean `git init`; the private product remotes and history are left untouched.
